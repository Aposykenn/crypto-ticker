# Crypto Ticker 📈

This project is a simple desktop application that tracks real-time price information for **Bitcoin (BTC)**, **Ethereum (ETH)**, and **Tether (USDT)**. It is built using **Electron JS** and fetches data via the **CoinGecko API**.

## Features

- **Live Price Tracking:** Displays current data for Bitcoin, Ethereum, and Tether.
- **Multi-Currency:** Shows prices in both US Dollars ($) and Turkish Lira (₺).
- **Auto-Refresh:** Data is automatically updated every 30 seconds.

## Installation

After cloning or downloading the project to your computer, navigate to the project directory in your terminal and run the following command to install the necessary dependencies:

```bash
npm install
```
